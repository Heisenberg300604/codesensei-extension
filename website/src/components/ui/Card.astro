---
interface Props {
  class?: string;
  hover?: boolean;
}

const { class: className = '', hover = false } = Astro.props;
const cardClasses = `glass rounded-2xl p-6 ${hover ? 'card-hover' : ''} ${className}`;
---

<div class={cardClasses}>
  <slot />
</div>
